# Быстрое руководство: Настройки для каждой модели отдельно

## ✅ Что исправлено

Настройки видимости групп теперь **изолированы для каждой модели**. 

Модель 2.9 и модель 3.3 имеют свои собственные настройки, которые не влияют друг на друга.

## 🚀 Как сбросить модель 3.3 (как вы просили)

### Вариант 1: Через UI (рекомендуется)

1. Откройте `/dev-settings`
2. Прокрутите до секции **"Group Visibility"**
3. Внизу найдите жёлтую панель **"⚠️ Model-Specific Settings"**
4. Нажмите кнопку **"🗑️ Reset 3.3"** (покажет количество настроек в скобках)
5. Подтвердите сброс
6. Нажмите **"💾 Save Group Visibility Settings"**

✅ Готово! Модель 3.3 сброшена до дефолта.

### Вариант 2: Через базу данных

Если нужно сбросить через SQL:

```sql
-- Удалить все настройки для модели 3.3
UPDATE visualization_settings
SET settings_data = jsonb_set(
  settings_data,
  '{mappings}',
  (
    SELECT jsonb_agg(mapping)
    FROM jsonb_array_elements(settings_data->'mappings') AS mapping
    WHERE mapping->>'modelId' != '3.3'
  )
)
WHERE settings_type = 'groups';
```

## 🎯 Как настроить разные модели

### Пример: Настройка модели 2.9

1. Выберите модель **2.9** в селекторе
2. В дереве объектов выберите группу `non_slip`
3. ✅ Включите "Hidden by default (for model 2.9)"
4. Выберите опцию "Soft deck"
5. Нажмите Save

### Пример: Настройка модели 3.3

1. Выберите модель **3.3** в селекторе
2. В дереве объектов выберите группу `pump`
3. ✅ Включите "Hidden by default (for model 3.3)"
4. Выберите опцию "Electric bilge pump"
5. Нажмите Save

Теперь на модели 2.9 управляется `non_slip`, а на 3.3 - `pump`, и они не конфликтуют!

## 📊 Панель управления

В Sandbox внизу секции Group Visibility теперь отображается:

```
⚠️ Model-Specific Settings
Currently editing: Model 2.9
Settings are isolated per model.

[🗑️ Reset 2.9 (3)]  [🗑️ Reset 2.9E (0)]
[🗑️ Reset 3.3 (5)]  [🗑️ Reset 3.3E (2)]
```

- **(3)** - количество настроенных групп для модели
- Кнопка серая если нет настроек
- Кнопка красная если есть настройки

## 🔄 Миграция данных

При первом применении миграции:
- Все существующие настройки будут привязаны к модели **2.9**
- Вы можете настроить другие модели отдельно
- Или сбросить любую модель через UI

### Применить миграцию

```bash
cd exce1sior-configurator
supabase db reset
```

## ✨ Итог

✅ Модели изолированы друг от друга  
✅ Модель 3.3 можно сбросить одной кнопкой  
✅ Каждая модель настраивается независимо  
✅ Существующие настройки сохранены (привязаны к 2.9)  

---

**Готово к использованию!**

