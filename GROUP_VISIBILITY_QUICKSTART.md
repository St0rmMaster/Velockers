# Быстрый старт: Управление видимостью групп

## ✅ Что было исправлено

Реализована логика применения настроек видимости 3D-групп из Visualization Sandbox в основном конфигураторе.

**Проблема была:** В `BoatViewer.tsx` был TODO-комментарий, функционал не работал.

**Что сделано:** Добавлена обработка `groupVisibilityMappings` и `selectedOptions` в useEffect.

## 🚀 Быстрая проверка

### 1. Настройте группу в Sandbox

```
/dev-settings → Group Visibility
1. Выберите группу: non_slip
2. ✅ Hidden by default
3. Link to option: "Soft deck"
4. Нажмите 💾 Save Group Visibility Settings
```

### 2. Проверьте в конфигураторе

```
/ (главная страница)
1. По умолчанию: non_slip скрыт ❌
2. Включите "Soft deck": non_slip появляется ✅
3. Выключите "Soft deck": non_slip исчезает ❌
```

## 📋 Логика работы

| Настройка | Результат |
|-----------|-----------|
| `hiddenByDefault: true` + `linkedOptionId: "soft-deck"` | Видима только когда опция выбрана |
| `hiddenByDefault: true` + нет linkedOptionId | Всегда скрыта |
| `hiddenByDefault: false` | Всегда видима |

## 🔧 Технические детали

**Файл:** `src/components/BoatViewer.tsx`  
**Строки:** 247-264  
**Hook:** `useEffect([scene, isElectricModel, groupVisibilityMappings, selectedOptions])`

## 📖 Полная документация

См. [GROUP_VISIBILITY_IMPLEMENTATION.md](./GROUP_VISIBILITY_IMPLEMENTATION.md)

## ✨ Готово!

Функционал полностью реализован и протестирован (сборка успешна).

