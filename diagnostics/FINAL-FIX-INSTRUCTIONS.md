# 🎯 Окончательное исправление Custom hull color

## Проблема
У вас было **ДВЕ записи** с именем "Custom hull color":
- ✅ `type='color'` (используется конфигуратором)
- ❌ `type='option'` (дубликат, не используется)

Вы редактировали **неправильную запись** (option), поэтому изменения не применялись!

---

## Решение

### Шаг 1: Выполните SQL скрипт

Откройте **Supabase Dashboard → SQL Editor** и выполните:

📄 **Файл:** `diagnostics/fix-duplicate-custom-color.sql`

Скрипт удалит дубликат и оставит только правильную запись (type='color').

---

### Шаг 2: Очистите кеш

**Вариант A (рекомендуется):**
1. Откройте конфигуратор
2. F12 → Console
3. Выполните:
   ```javascript
   localStorage.removeItem('exce1sior.catalog-cache.v1')
   location.reload()
   ```

**Вариант B:**
- F12 → Application → Local Storage → удалите ключ `exce1sior.catalog-cache.v1`

---

### Шаг 3: Редактируйте ПРАВИЛЬНУЮ запись

⚠️ **ВАЖНО:** Теперь редактируйте Custom hull color на вкладке **"Colours"**, а НЕ "Options"!

1. Админ-панель → **Colours** (не Options!)
2. Найдите "Custom hull color"
3. Измените цену
4. Сохраните
5. Обновите конфигуратор → изменения применятся! ✅

---

## Почему так?

Custom hull color - **особая опция**:
- Хранится как `type='color'` с `kind='custom'`
- Показывается в секции "Select Color" (не в "Options")
- Имеет специальную логику загрузки через `buildColourData()`

Запись с `type='option'` была **ошибочным дубликатом**, который не использовался системой.

---

## Проверка после исправления

```sql
-- Должна быть только ОДНА запись
SELECT 
  type, name, price_usd, 
  metadata->>'priceUsd' as metadata_price_usd
FROM products 
WHERE name ILIKE '%custom%hull%color%';
```

**Ожидаемый результат:**
```
type  | name              | price_usd | metadata_price_usd
------|-------------------|-----------|-------------------
color | Custom hull color | 350.00    | 350
```

---

## ✅ Итог

После выполнения:
- ✅ Только одна запись Custom hull color (type='color')
- ✅ Редактирование на вкладке "Colours" работает
- ✅ Изменения сразу видны в конфигураторе
- ✅ Нет дубликатов и путаницы

