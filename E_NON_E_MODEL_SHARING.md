# Группировка моделей E/non-E: Общие настройки видимости

## 🎯 Изменение

Модели **2.9 и 2.9E** теперь делят одни и те же настройки видимости групп.
Модели **3.3 и 3.3E** также делят одни и те же настройки.

## ✅ Что это значит

### Для пользователя

Когда вы настраиваете видимость групп для модели **2.9**, эти же настройки автоматически применяются и к **2.9E**.

**Пример:**
1. Выбрали модель 2.9 в Sandbox
2. Настроили группу `non_slip` → привязали к опции "Soft deck"
3. Переключились на модель 2.9E
4. ✅ Группа `non_slip` **автоматически** связана с "Soft deck" и на 2.9E

### Разница между E и non-E моделями

Единственная разница — **автоматически управляемые группы**:
- `el_motor` - видим только на E моделях
- `battary_V_4_new` - видима только на E моделях

Эти группы управляются кодом и **не требуют настройки**.

### Изоляция между разными моделями

Модели 2.9/2.9E и 3.3/3.3E **полностью изолированы** друг от друга:
- Настройки для 2.9 → применяются к 2.9 и 2.9E
- Настройки для 3.3 → применяются к 3.3 и 3.3E
- Настройки 2.9 **НЕ влияют** на 3.3

## 🔧 Техническая реализация

### Функция getBaseModelId

```typescript
export function getBaseModelId(modelId: string): string {
  return modelId.replace(/E$/, '');
}

// Примеры:
getBaseModelId('2.9')  // → '2.9'
getBaseModelId('2.9E') // → '2.9'
getBaseModelId('3.3')  // → '3.3'
getBaseModelId('3.3E') // → '3.3'
```

### Применение в конфигураторе

```typescript
// BoatViewer.tsx
const baseModel = getBaseModelId(modelId);
const mapping = groupVisibilityMappings.find(
  m => m.groupName === childName && m.modelId === baseModel
);
```

### Сохранение в Sandbox

```typescript
// DevSettingsPage.tsx
const baseModel = getBaseModelId(modelId);
// Всегда сохраняем по базовой модели
return [...prev, { groupName, modelId: baseModel, ... }];
```

## 📊 Структура данных

### В базе данных

```json
{
  "mappings": [
    {
      "groupName": "non_slip",
      "modelId": "2.9",  // ← используется и для 2.9, и для 2.9E
      "hiddenByDefault": true,
      "linkedOptionId": "soft-deck"
    },
    {
      "groupName": "pump",
      "modelId": "3.3",  // ← используется и для 3.3, и для 3.3E
      "hiddenByDefault": true,
      "linkedOptionId": "electric-bilge-pump"
    }
  ]
}
```

## 🎨 UI в Visualization Sandbox

### Индикация текущей группы

```
Hidden by default (for 2.9 & 2.9E)
```

При выборе любой модели (2.9, 2.9E, 3.3, 3.3E) UI показывает, что настройки применяются к обеим вариантам.

### Панель управления

```
ℹ️ Model Groups (E/non-E share settings)

Currently editing: Model 2.9E (uses 2.9 settings)
Electric models (E) and non-electric models share the same visibility settings.
Only motor and battery groups are managed automatically.

[🗑️ Reset 2.9 & 2.9E (3)]  [🗑️ Reset 3.3 & 3.3E (0)]
```

- **2 кнопки** вместо 4
- Каждая сбрасывает обе версии модели (E и non-E)
- Показывает общее количество настроек для группы

## ✨ Преимущества

### 1. Консистентность

Пользователь видит **одинаковое поведение** при переключении между 2.9 и 2.9E в конфигураторе.

### 2. Упрощение настройки

Не нужно настраивать одно и то же дважды. Настроил для 2.9 — автоматически работает и для 2.9E.

### 3. Избежание ошибок

Невозможна ситуация, когда на 2.9 группа привязана к одной опции, а на 2.9E — к другой.

### 4. Изоляция между моделями

При этом 2.9 и 3.3 остаются полностью независимыми (решает проблему с одинаковыми названиями мешей).

## 🚀 Использование

### Настройка для модели 2.9

1. Выберите **любую** модель: 2.9 или 2.9E
2. Настройте группы как обычно
3. ✅ Настройки применятся к обеим моделям

### Настройка для модели 3.3

1. Выберите **любую** модель: 3.3 или 3.3E
2. Настройте группы как обычно
3. ✅ Настройки применятся к обеим моделям

### Сброс настроек

**Сбросить 2.9 и 2.9E:**
- Нажмите "🗑️ Reset 2.9 & 2.9E"
- Сбросятся настройки для обеих моделей

**Сбросить 3.3 и 3.3E:**
- Нажмите "🗑️ Reset 3.3 & 3.3E"
- Сбросятся настройки для обеих моделей

## 📋 Миграция данных

Если у вас были отдельные настройки для E моделей:

### Вариант 1: Автоматическое объединение (будущее)

Можно создать скрипт миграции, который:
1. Берёт настройки для 2.9E
2. Объединяет их с настройками для 2.9
3. Удаляет дубликаты
4. Оставляет только настройки с modelId='2.9'

### Вариант 2: Ручная очистка (текущее)

1. Откройте Sandbox
2. Проверьте настройки для 2.9
3. Если нужно, скопируйте настройки с 2.9E
4. Нажмите "🗑️ Reset 2.9E" (если такая кнопка есть)
5. Сохраните

Примечание: После обновления modelId='2.9E' больше не создаются, поэтому проблема решается автоматически для новых настроек.

## 🔍 Проверка работы

### Тест 1: Группировка работает

1. Выберите модель **2.9** в конфигураторе
2. Включите опцию "Soft deck"
3. Группа `non_slip` появляется ✅
4. Переключитесь на **2.9E**
5. Группа `non_slip` **всё ещё видна** ✅

### Тест 2: Изоляция работает

1. Настройте группу `non_slip` для 2.9
2. Переключитесь на модель **3.3**
3. Группа `non_slip` **не реагирует** на настройки 2.9 ✅

### Тест 3: Автоматические группы

1. Выберите модель **2.9** (non-E)
2. `el_motor` и `battary_V_4_new` **скрыты** ✅
3. Переключитесь на **2.9E**
4. `el_motor` и `battary_V_4_new` **видимы** ✅

## 📝 Изменённые файлы

### 1. BoatViewer.tsx
- Добавлена функция `getBaseModelId()`
- Экспортирована для использования в других компонентах
- Логика применения использует базовую модель

### 2. DevSettingsPage.tsx
- Импортирована функция `getBaseModelId()`
- Обработчики используют базовую модель
- UI показывает группировку (2.9 & 2.9E)
- Панель управления показывает 2 кнопки вместо 4

### 3. types/index.ts
- **Не изменён** - поле `modelId` остаётся как есть
- Просто хранятся только базовые значения ('2.9', '3.3')

## ✅ Итог

✅ **2.9 и 2.9E делят настройки**  
✅ **3.3 и 3.3E делят настройки**  
✅ **Изоляция между 2.9 и 3.3 сохранена**  
✅ **Мотор и батарея управляются автоматически**  
✅ **UI обновлён и понятен**  
✅ **Сборка успешна**  

---

**Готово к использованию!** 🚀

**Дата:** 29 октября 2025  
**Обновление:** E/non-E model grouping  
**Статус:** ✅ COMPLETE

